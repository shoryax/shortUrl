a bit.ly copy


        Receive URL
            ↓
        Validate URL
            ↓
        Generate short code
            ↓
        Check if code already exists
            ↓
        If it exists → generate another
            ↓
        Store in database
            ↓
        Return short code


        the shorten/route.ts
            Receive request
                    ↓
            Read JSON body
                    ↓
            Call shortenService
                    ↓
            Return JSON
    